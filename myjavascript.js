// javascript fill : my first script .js
function StringLength(){
    // retrieving the values of from element
    var name = document.getElementById("name").value;
    document.getElementById("length").innerText = name.length
}